#include "iniconds_generator.hpp"

void gaussian_sphere() {
    /*
    Generates a Gaussian-distributed 3D particle sphere with optional solid-body rotation.

    Arguments:
    - `N`: Number of particles
    - `axis`: Unit vector defining the rotation axis 
    - `Ω_frac`: Angular velocity in radians per unit time (default: 0.0)
    - `rng`: Optional RNG seed

    Returns:
    - `pos`: NxDims matrix of particle positions
    - `vel`: NxDims matrix of particle velocities
    */

}


// --------------------------------
//      Generate iniconds 
// --------------------------------
int main(int argc, char* argv[]) {

    return 0;
}